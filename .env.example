# Optional: required only for paid Full Research Mode.
PARATERA_API_KEY=replace-with-your-api-key

# Optional UI/runtime overrides. The deterministic Demo Mode needs none of these.
PATH_AI_SCIENTIST_STATE_ROOT=state/workflow
